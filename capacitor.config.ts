import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app789',
  appName: 'photo-sharing-app',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
